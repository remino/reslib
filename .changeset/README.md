# Changesets

Add one markdown file per package change with `npx changeset`.

Keep each changeset focused on the package(s) that changed and the bump type
you want. `npm run version:packages` will apply them and refresh the lockfile.
