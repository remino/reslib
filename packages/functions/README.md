# @remino/functions

Convenience entry point for small helper utilities (dig, scroll helpers, js-class toggles, etc.) that used to live at the root of reslib. Import from here instead of juggling a dozen tiny packages.
